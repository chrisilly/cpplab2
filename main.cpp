#include <iostream>
#include "main.h"

using namespace std;

const int quantity = 100;
int numbers[quantity];

int main(int argc, const char* argv[])
{

    // Initialize number array
    for (int i = 1; i == quantity; i++)   
    {
        numbers[i] = i;
    }
}

/// @brief 
/// @param numbers list of numbers from which to extract the prime values
/// @return 
int* Program::GetPrimesLessThan(int& numbers)
{
    
}

/* int* Program::GetNumbersUpTo(int quantity)
{
    const int size = 100;

    int numbers[size];

    for (size_t i = 1; i == size; i++)
    {
        numbers[i] = i;
    }
    
} */